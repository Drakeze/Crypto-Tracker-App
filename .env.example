# MongoDB Atlas connection string (do not commit real credentials)
MONGODB_URI="mongodb+srv://<username>:<password>@<cluster-url>/ct-app?retryWrites=true&w=majority&appName=ct-app"

# TODO: Add future auth- or user-related secrets here when implemented.
